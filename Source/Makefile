ifeq ($(strip $(SATURN_SAMPLE_COMMON)),)
$(error "Please set SATURN_SAMPLE_COMMON in your environment.  This should point to the root of the common directory." )
endif

export SAMPLE=LD34

include $(SATURN_SAMPLE_COMMON)/Source/Makefile
